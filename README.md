# DatadogTracingExamples

Two example projects

## 1 python

very simple webapp which will generate different number of spans depending on the URL which is called.

## 2 Nodejs

example of nodejs which will pass the parent span ID through SQS so that both sides of the application are appearing in the same trace
