import { ExportResult } from '@opentelemetry/core';
import { ReadableSpan, SpanExporter } from '@opentelemetry/tracing';
import { DatadogExporterConfig } from './types';
/**
 * Format and sends span information to Datadog Exporter.
 */
export declare class DatadogExporter implements SpanExporter {
    private readonly _logger;
    private readonly _exporter;
    private _serviceName;
    private _env;
    private _version;
    private _tags;
    private _url;
    private _flushInterval;
    private _isShutdown;
    constructor(config?: DatadogExporterConfig);
    /**
     * Called to export sampled {@link ReadableSpan}s.
     * @param spans the list of sampled Spans to be exported.
     */
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    /** Stops the exporter. */
    shutdown(): Promise<void>;
}
//# sourceMappingURL=datadog.d.ts.map