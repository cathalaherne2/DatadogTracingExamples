import { Sampler, SamplingResult } from '@opentelemetry/api';
/** Sampler that samples a given fraction of traces but records all traces. */
export declare class DatadogProbabilitySampler implements Sampler {
    private readonly _probability;
    constructor(_probability?: number);
    shouldSample(context?: unknown): SamplingResult;
    toString(): string;
    private _normalize;
}
export declare const DATADOG_ALWAYS_SAMPLER: DatadogProbabilitySampler;
export declare const DATADOG_NEVER_SAMPLER: DatadogProbabilitySampler;
//# sourceMappingURL=datadogProbabilitySampler.d.ts.map